package network;

import java.net.Socket;

public class ProtocolClient {
	private Socket socket;
	
	public ProtocolClient() {
		
		//socket = new Socket("서버IP", port);
		socket = new Socket("서버IP", 9500);
	}
	
	public static void main(String[] args) {
		new ProtocolClient();
	}
}
